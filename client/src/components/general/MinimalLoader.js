import { Center, Loader } from '@mantine/core';

const MinimalLoader = () => {
    return (
        <Center sx={{ height: '80vh' }}>
            <Loader />
        </Center>
    );
};

export default MinimalLoader;
